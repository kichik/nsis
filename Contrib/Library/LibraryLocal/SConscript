target = 'LibraryLocal'

files = Split("""
	LibraryLocal.cpp
	#Source/ResourceEditor.cpp
	#Source/winchar.cpp
""")

libs = Split("""
	kernel32
	oleaut32
	version
""")

Import('BuildUtil')

BuildUtil(target, files, libs, flags = ['$EXCEPTION_FLAG'], cross_platform = True)
